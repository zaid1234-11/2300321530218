# Notification System

## Architecture

The system fetches notifications from the API and displays them to users.

## Features

- View all campus notifications
- Filter by notification type (Placement, Result, Event)
- Pagination support
- Mark notifications as read

